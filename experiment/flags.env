# Flags da variação atual do experimento.
# São lidas pelo workflow e usadas pelo gerador de testes (scripts/gen_tests.py).
# O driver scripts/run_experiment.py reescreve este arquivo a cada variação.
VARIATION=baseline
USE_PIP_CACHE=true
EXTRA_TESTS=0
INCLUDE_FAILING=false
INCLUDE_SLOW=false
SLOW_SECONDS=0
